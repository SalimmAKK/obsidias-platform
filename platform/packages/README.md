# platform/packages

Reserved for code shared between `platform/frontend` and `platform/api` (e.g. shared
TypeScript types, a queue client, a DB client). Empty for now — nothing was extracted
here during the restructure to avoid touching working feature code.
